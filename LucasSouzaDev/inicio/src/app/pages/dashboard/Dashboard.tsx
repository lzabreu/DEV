import { Link } from 'react-router-dom';
import { Menu } from '../../components/Menu';

export const Dashboard = () => {
    return (
        <div>
            <Menu />
            <p>Dashboard</p>
            
        </div>
    )
}